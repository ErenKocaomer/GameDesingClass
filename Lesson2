import sys
from math import trunc
import pandas as pd
import pandas as np

print(sys.version)
#shift + alt + e = sadece o satır

# Pyton Variables
# Variables are used to store data values
#
#

def multiplication(x , y):
    """

    :param x:
    :param y:
    :return:
    """
    print(x * y)

toplam = 5.4 + 5

if(toplam > 10):
    print(str(toplam) + " WOW")

else: print(str (toplam) + " LOW")

x = float(10)
print(type(x))


fruits = ["apple" , "Cherry" , "Banana"]
x , y , z = fruits
print(x, z, z)

u = 19
def myfunction():
    print("python " + str(u))

myfunction()
